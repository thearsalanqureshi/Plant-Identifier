// Feature card sizes
enum CardSize { large, small }

// Feature card types for different functionalities
enum CardType { identify, diagnose, water, light }

// App themes
enum AppTheme { light, dark }

// Language options
enum AppLanguage { english, urdu, german, french, arabic, spanish, japanese, korean }

// Navigation destinations
enum NavDestination { home, identify, diagnose, history, settings }

enum ScannerMode { identification, diagnosis, waterCalculation }